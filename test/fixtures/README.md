# Test Fixtures

Place `production-sample.json` here after running `scripts/sample-production-data.js` in the Alba console.

This file is gitignored (contains PII-sanitized but still real address data).
